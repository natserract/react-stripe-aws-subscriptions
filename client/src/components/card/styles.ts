import { Theme, createStyles } from "@material-ui/core/styles";

export default (_theme: Theme) =>
  createStyles({
    root: {
      maxWidth: 345,
    },
    media: {
      height: 140,
    },
  });
